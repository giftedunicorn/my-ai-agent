# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# ============================================
# Database Configuration
# ============================================
# PostgreSQL database connection string
# You can use Vercel Postgres, Supabase, or local PostgreSQL
# Example for Supabase:
POSTGRES_URL="postgres://postgres.[USERNAME]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?workaround=supabase-pooler.vercel"

# Example for Vercel Postgres (will be auto-configured if you use Vercel):
# POSTGRES_URL="postgres://default:xxx@xxx.postgres.vercel-storage.com:5432/verceldb"

# Example for local PostgreSQL:
# POSTGRES_URL="postgresql://username:password@localhost:5432/chatbot"

# ============================================
# AI Configuration
# ============================================
# Google Gemini API Key for LangChain Agent
# Get your API key from https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=''